-- Ventros CRM - Database Initialization
-- This file only sets up extensions.
-- All tables are managed by Ent/Domain migrations (make db-sync)

-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
